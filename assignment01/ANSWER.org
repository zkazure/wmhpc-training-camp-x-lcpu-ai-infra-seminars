* Environment

- GPU 型号            :: NVIDIA GeForce RTX 4090 D
- compute capability  :: 8.9
- SM 数量             :: 114
- warp 大小           :: 32
- shared mem / block  :: 49152
- max threads / SM    :: 1536
- global mem          :: 25252724736
- max threads / block :: 1024


* 为什么要用 GPU

1. prob
   - 不对，因为 100 TFLOPS 实际上是最大的吞吐量，和单条指令的延迟并不是同一个概念。
     + [X] FLOP 是一次浮点运算，FLOPS 是每秒浮点运算。
     + [X] T 是什么单位。\( 1 T = 10 ^12 \)
   - 正确。
   - 正确。
   - 不对。
2. GPU 的 FLOP 指标指的是所有线程全速运行的速度。
   但是对于一个严格的串行算法，同一时刻没有足够多的独立运算可以并行。
   而且受到单步延迟的影响。
3. 填表。
   | 执行层次       | thread                | warp                                    | block/CTA                                    | grid                                  |
   | 软件含义       | kernel 的最小执行单位 | 一个 block 中连续的 32 个 thread 的组合  | 一组高效协作的 threads                       | 一次 kernel 创建的全部 threads blocks |
   | 对应硬件       | 计算单元上的一个 lane | 由 SM 的 warp scheduler 调度的一组 lane | 同一个 block 的 thread 需要在同一个 SM 上执行 | 可用的 SM                             |
   | 直接可用的存储 | 自己的寄存器          | 没有直接的 “warp memory”                | shared memory                                | global memory                         |
   | 同步与通信手段 | （自身天然有序）      | __syncwarp(), __shfl_sync()...          | __syncthreads()...                           | kernel 边界                           |
4. SIMD：single instruction multiple data，一个指令操作多个数据（向量化）。
   SIMT: single instruction multiple threads，每个线程跑一样的代码，但是数据、状态可能不同。
   不对。branch divergence 会让一个 warp 需要分开跑不同的分支，有性能代价。
5. experiment.
   | 配置             | 耗时 ms | ns/元素 |
   |------------------+---------+---------|
   | CPU 单线程       |   7.085 |    1.69 |
   | GPU <<<1, 1>>>   | 133.670 |   31.87 |
   | GPU <<<1, 256>>> |   2.060 |    0.49 |
   | GPU 铺满 grid    |   0.022 |    0.01 |
   - GPU 的延迟高，需要靠多线程掩盖。
     CPU 乱序执行、分支预测。
   - 多线程并行和调度，实现的数据并行计算和延迟掩盖。


* 第一个 CUDA 程序

1. finished
2. prob.
   - ~__global__~
   - ~__device__~
   - ~__host__ __device__~
   - ~__constant__~
   - ~__shared__~
3. modify.
   #+begin_example
   搬运 + kernel + 读回: 115.8 ms
   搬运 + kernel + 读回: 95.8 ms
   #+end_example
   - kernel 启动和 CPU 是异步的。
     原先的版本的同步在 kernel 启动后 check kernel 里面。
   - 和使用的卡有关系。
4. concept。
   - 不对。
     kernel 启动对 host 是异步的。
   - *正确* 。
     同一个 stream 中的操作按提交顺序执行。
   - 不会。
5. end
6. modify.
   价值在于同一个 kernel 启动的参数可以适配所有的 n。
   每个线程需要处理 n/grid.size 个线程并行，把并行限制在了 grid.size()，可能有资源的空闲。
